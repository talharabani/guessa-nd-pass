# ──────────────────────────── Number Rush — web ────────────────────────────
# Copy to .env.local for development. On Vercel, set these in Project Settings
# → Environment Variables. NEXT_PUBLIC_* values are exposed to the browser.

# Where the Express + Socket.IO backend lives.
# Local:      http://localhost:4000
# Production: https://number-rush-server.onrender.com
NEXT_PUBLIC_SERVER_URL=http://localhost:4000
